# hEX + OpenWrt — зависание после ~2 суток аптайма (расследование начато 2026-09-09)

> Продолжать отсюда. Реальные пароль/IP — в `CREDENTIALS.local.md` (не в git).
> Доступ: `ssh -i ~/.ssh/id_ed25519 root@192.168.1.1` (ключ ПК2 добавлен 2026-09-09).

## Симптом

Роутер проработал непрерывно ~2 дня 5 часов, завис, потребовалась перезагрузка
по питанию. Пользователь подозревает заполнение оперативной памяти за несколько
дней непрерывной работы.

## Что установлено 2026-09-09 (вечер)

- **Аптайм до отказа:** загрузка `2026-09-07 12:32` → зависание → power-cycle
  `2026-09-09 ~17:48`. Источник: `/var/log/passwall2.log` (запись «Start after a
  delay of 60 seconds!» от 2026-09-07 12:31:59, следующая — только 2026-09-09
  17:49) и `/proc/uptime`.
- **Причину задним числом не восстановить.** Syslog OpenWrt живёт в RAM-буфере
  128 КБ (`logd -S 128`) и стёрт перезагрузкой. На флешке (overlay) переживает
  ребут только `/var/log/passwall2.log`, а там лишь старт/стоп сервиса.
- **Состояние через 22 мин после загрузки — здоровое, это точка отсчёта:**

  | метрика | значение |
  |---|---|
  | MemAvailable | 133–138 МБ из 248 |
  | xray RSS | ~48 МБ, 12 потоков, ~150 fd |
  | /tmp (tmpfs) | 2 МБ из 121 |
  | conntrack | ~470 / 31744 |
  | dnsmasq_default | 1 процесс |
  | overlay (флешка) | 120 МБ / 3.6 ГБ (3%) — к делу не относится |
  | туннель | жив: `curl ifconfig.me` → `<SERVER_IP>`, colo=FRA, loc=DE |

- **Все известные фиксы из заметок на месте и держатся:** `fingerprint=safari`,
  `net.ipv4.tcp_max_orphans=8192`, `nf_conntrack_tcp_timeout_established=7440`,
  патч `util_xray.lua` (`allowedNetwork`→`network`, 0 вхождений старого),
  `log_node=0`, `psw2-udp-guard` помечен `#DISABLED` в `/etc/crontabs/root`.
  → Это **новый, неразобранный** отказ, а не откат старого.
- **Часы:** RTC-батарейки нет. При загрузке 2026-09-09 время восстановилось на
  timestamp предыдущей загрузки (отставание ~53 ч), NTP выправил за ~2 мин.
  Персист времени слабый. Не причина зависания, но кандидат на укрепление.

## Рабочая гипотеза (2026-09-09, ОПРОВЕРГНУТА 2026-09-14 — см. ниже)

Медленный рост RSS у `xray` (Go-процесс: сотни отдельных TCP к серверу без mux
+ geoip-проверка каждого соединения из-за RU-шунта `rulenode`) → под срок «пара
суток» упирается в свободную RAM → OOM-killer убивает `xray`/`dnsmasq` → VPN и
DNS падают → «надо перезагрузить». Совпадает с неразобранным пунктом старых
заметок: *«туннель деградирует со временем, ресурсы при этом на вид в норме»*.

Альтернативы (проверять по логу memwatch):
- заполнение conntrack тысячами короткоживущих проксируемых соединений;
- дубли `dnsmasq_default` после серии рестартов PassWall2;
- `psw2-portguard` (cron, раз в 5 мин) дёргает неудачный рестарт PassWall2.

**Оказалось не так** — см. раздел «2026-09-14: пойман живой коллапс» ниже.
Дело не в плавном многодневном дрейфе, а в быстром (минуты) всплеске.

## Что сделано для диагностики

Поставлен `/root/memwatch.sh`, cron `*/3 * * * *`:
- дописывает в `/root/memwatch.log` (самоочистка при 400 КБ, переживает ребут):
  дата, аптайм, MemAvailable, MemFree, SUnreclaim, /tmp, xray RSS, xray fd,
  conntrack count/max, число `dnsmasq_default`, load;
- вытаскивает из RAM-лога строки OOM-killer / lowmem / allocation failure, пока
  они ещё живы.

Первый снимок:
```
2026-09-09 18:14:48 up=1595s availMB=133 freeMB=94 slabU=23004k tmp=2116k
                    xrayRSS=48992k xrayFd=146 ct=409/31744 dnsmasq=1 load=1.39
```

## 2026-09-14: пойман живой коллапс

Роутер держал ту же сессию с 09-09 ещё ~4.5 суток без проблем (avail стабильно
~137–140 МБ, xray RSS ~40–44 МБ, ct ~80–200) — то есть версия про плавный
многодневный дрейф **не подтвердилась**: система была здорова почти 5 суток,
а не тихо ползла к отказу. Обвал произошёл резко:

| время (UTC) | avail МБ | xray RSS | xray fd | conntrack | load |
|---|---|---|---|---|---|
| 10:24–10:33 | ~137–140 | ~40–44 МБ | 34–102 | 80–200 | ~0 |
| **10:36** | 105 | 71 МБ | — | **3754** | 2.24 |
| 10:39 | 111 | 68 МБ | — | 7031 | 3.41 |
| **10:42** | **25** | **146 МБ** | **3881** | 8247 | 2.83 |
| 10:46–11:24 (39 мин) | **0** | ~147–153 МБ | **зависло на 4680** | 200–400 | **11–17** |
| 11:24 → 11:32 | — тишина, тики cron растянулись с 3 мин до 9 мин, затем ушёл в перезагрузку — |||||

**От нормы до `avail=0` — 6 минут.** Дальше 39 минут: load 11–17 на
двухъядерном MIPS — фактический паралич, даже cron не мог выполниться вовремя.
Заодно поймали автоматический рестарт PassWall2 в 11:15 — обычно ~30 сек, тут
растянулся на 18 минут (11:15:45 → 11:33:37 в `passwall2.log`), что
подтверждает полную парализацию системы, а не просто «подтормаживает».

**Ключевая улика:** `xray fd` простоял на **ровно 4680** все 40 минут агонии —
даже когда `conntrack` (реальные сетевые соединения) сам спал с 8247 до
200–400. Xray не отпускал дескрипторы/память от уже закрывшихся соединений.
Похоже на утечку fd/памяти в xray **под всплеском нагрузки**, а не на
постоянную капель. `ulimit` — не причина (65535, до него далеко).

В тот же день **меньший похожий всплеск в 15:09–15:39** (ct до 4825, avail не
ниже 109, load до 4.0) откатился сам — паттерн воспроизводимый, но не каждый
всплеск фатален.

**Не удалось установить:** что именно провоцирует всплеск — conntrack не
хранит историю, посттравматический список соединений выглядит обычным
(443/53/бытовые порты), на DHCP 9 обычных устройств, торрент-паттерна не
видно. `psw2-portguard` в тот день (переключение порта ноды 2053→443 в 13:15)
— штатная работа, к обвалу отношения не имеет.

## 2026-09-15: поставлен автосторож memguard

Раз причина коллапса — не дрейф, а всплеск за единицы минут, ждать и ловить
руками бессмысленно. Поставлен `/root/memguard.sh`, cron **раз в минуту**
(лёгкий: только чтение `/proc`, без сетевых вызовов, кроме момента лечения).

Пороги подобраны так, чтобы попадать между безобидным всплеском 15:09 (avail
не ниже 109, ct до 4825) и настоящим коллапсом 10:36 (avail к 25, ct к 8247,
fd к 4680):

| | WARN (только в лог) | CRIT (действие) |
|---|---|---|
| MemAvailable | < 120 МБ | **< 100 МБ** |
| conntrack | > 2000 | **> 6000** |
| xray fd | > 500 | **> 2000** |

При CRIT: пишет в `/root/memguard.log` полный снимок + строки OOM из RAM-лога
→ пробует дёшево `drop_caches`+`compact_memory`, ждёт 5 с, перепроверяет → если
не помогло, `/etc/init.d/passwall2 restart`, ждёт до 60 с, проверяет туннель
curl'ом → кулдаун 15 минут между вмешательствами. Молчит, пока всё в норме.

Проверено: синтаксис ОК, cron реально запускает (`crond` в `logread`), первый
`--check` на здоровом роутере → `avail=136MB ... -> OK`.

## Следующий шаг — с этого продолжать

1. Ждать: либо memguard сам поймает и вылечит следующий всплеск (смотреть
   `/root/memguard.log` за записями CRIT), либо всплеск окажется настолько
   быстрым/сильным, что даже минутный интервал не спасёт — тогда разбирать
   `memwatch.log` вокруг этого момента заново.
2. **Найти триггер всплеска** — это главный открытый вопрос. При следующем CRIT
   в `memguard.log` сразу проверять, кто в сети активен
   (`cat /tmp/dhcp.leases`, `wc -l /proc/net/nf_conntrack`, топ destination IP
   по `awk` — см. команды в истории чата) — за первые секунды после
   срабатывания, пока конntrack не успел спасть.
3. Если утечка fd у xray подтвердится ещё раз — включить `mux` в ноде
   `LgCRCnyE`/`rulenode`, это должно уменьшить число отдельных дескрипторов на
   клиентское соединение.
4. Часы: персист слабый (см. ниже), не блокер, но стоит укрепить.

## Опционально к укреплению (не срочно)

- Персист времени / быстрый NTP-фикс по голым IP уже есть — проверить, что
  hotplug сохраняет время чаще (было расхождение ~53 ч при загрузке 09-09).
- `/etc/dropbear/authorized_keys` строка 2 — битый обрезок ключа от неудачного
  `>>`, безвреден, можно вычистить.
